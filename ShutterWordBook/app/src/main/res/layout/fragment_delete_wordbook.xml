<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_lightblue"

    android:id="@+id/frag_deletewordbook"
    tools:context="org.androidtown.shutterwordbook.Fragment.DeleteWordbookFragment">
    <!-- DB 파일의 내용을 출력할 리스트뷰 - -->


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#ff972730"
        >

        <Button
            android:id="@+id/button_delete"
            android:layout_width="120dp"
            android:layout_height="30dp"
            android:text="- 삭제"
            android:layout_gravity="right"
            android:layout_marginRight="40dp"
            android:layout_marginTop="20dp"
            android:textColor="@color/color_white"
            android:background="@color/color_deepblue"
            />
        <ListView
        android:id="@+id/listView_wordbooks"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:background="#FFFFFF"
        android:choiceMode="singleChoice"
        android:layout_marginTop="20dp"
        />
    </LinearLayout>
</FrameLayout>

